# Example environment variables for voting-client (Vite)
# Copy to .env.local for local development (do NOT commit secrets)

# Base URL of the API server used by the client
VITE_API_URL=http://localhost:5000
