apiVersion: v1
kind: Service
metadata:
  name: voting-client
  labels:
    app: voting-client
spec:
  type: NodePort
  selector:
    app: voting-client
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
